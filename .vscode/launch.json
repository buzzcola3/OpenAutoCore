{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug autoapp",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/autoapp",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "miDebuggerPath": "${workspaceFolder}/runnec-gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "build-autoapp",
      "externalConsole": false
    }
  ]
}
