genrule(
    name = "glibconfig_header",
    outs = ["glibconfig.h"],
    cmd = "cp /usr/lib/x86_64-linux-gnu/glib-2.0/include/glibconfig.h $@",
)

cc_library(
    name = "glibconfig",
    hdrs = [":glibconfig.h"],
    includes = ["."],
    visibility = ["//visibility:public"],
)
